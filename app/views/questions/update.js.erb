<% if @question.errors.blank? %>
  $('#question').replaceWith('<%= j render @question %>');
<% else %>
  $('#question-<%= @question.id %>-errors').html('<%= j render 'shared/short_errors', resource: @question %>');
<% end %>
