h1 = @question.title
p = @question.body

- if current_user&.author?(@question)
  p = link_to('Delete', question_path(@question), method: :delete, data: {confirm: 'Are you sure?'}, class: 'btn btn-danger')

p = link_to 'Back', questions_path
